version: '3'

services:

  portainer:
    image: portainer/portainer:1.22.2
    environment:
      - HOST
    container_name: portainer
    command: -H unix:///var/run/docker.sock
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    labels:
      - traefik.frontend.rule=Host:${HOST}/portainer
      - traefik.port=9000
    networks:
      - proxy

  traefik:
    image: traefik:1.7.18
    container_name: traefik
    environment:
      - HOST
    command: --docker --docker.domain=${HOST}/docker
    ports:
      - 80:80
      - 443:443
    networks:
      - proxy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${PWD}/traefik_${HOST}.toml:/traefik.toml
      - ${PWD}/acme.json:/acme.json
    labels:
      - traefik.frontend.rule=Host:${HOST}/traefik
      - traefik.port=8080
    restart: always

  mysql:
    image: mysql:5.7
    container_name: mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATA_DIR: ${MYSQL_DATA_DIR}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - ${MYSQL_DATA_DIR}:/var/lib/mysql
    networks:
      - keycloak
    ports:
      - 3306:3306

  keycloak:
    image: jboss/keycloak:7.0.0
    container_name: keycloak
    restart: unless-stopped
    environment:
      DB_VENDOR: MYSQL
      DB_ADDR: mysql
      DB_DATABASE: keycloak
      DB_USER: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
      KEYCLOAK_USER: ${KEYCLOAK_USER}
      KEYCLOAK_PASSWORD: ${KEYCLOAK_PASSWORD}
    networks:
      - keycloak
      - proxy
    ports:
      - 9090:8080
    depends_on:
      - mysql
    labels:
      - traefik.frontend.rule=Host:${HOST}
      - traefik.port=9090

volumes:
  portainer_data:

networks:
  keycloak:
  proxy: